pipeline {
    agent {
        label 'izar-orliac'
    }

    stages {
        stage('Testing') {
            steps {
                //sh  'sbatch ./blah.sh'
		sh 'pwd'
		sh 'ls -rlth'
            }
        }
    }
}
